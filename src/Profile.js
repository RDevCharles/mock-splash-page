//profile card for users. Not being utilzed.

import ProfileCard from "./components/ProfileCard";
const Profile = () => {
    return (
        <div>
            <ProfileCard/>
    </div>
)
}

export default Profile;